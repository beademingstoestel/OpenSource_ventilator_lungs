import React from 'react';
import cx from 'classnames';
import MasterLayout from '../components/master-layout';

const MachinesProfile = ({ className, ...other }) => {
    return (
        <MasterLayout>
            Machines profile page
        </MasterLayout>
    );
};

export default MachinesProfile;
