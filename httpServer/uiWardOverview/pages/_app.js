import 'ventilator-lungs-ui/scss/index.scss';

const CustomApp = ({ Component, pageProps }) => {
    return (
        <>
            <Component {...pageProps} />
        </>
    );
};

export default CustomApp;
